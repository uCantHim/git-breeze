# git-breeze

A custom implementation of a git helper, inspired by `https://github.com/scmbreeze/scm_breeze`. I don't like the fish
port (some commands just don't work properly) plus I want to get acquainted with Rust.
